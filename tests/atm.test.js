const ATM = require('../lib/atm')

describe('ATM', () => {
  var atm
  beforeEach(() => {
    atm = new ATM()
  })
  describe('#Withdrawal', () => {
    xtest('Requests withdraw from bank', () => {
      //assesses whether withdraw has been called on the bank mock
    })
  })

  describe('#Deposit', () => {
    xtest('Requests deposit from bank', () => {
      //assesses whether withdraw has been called on the bank mock
    })
  })
})
